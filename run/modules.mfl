container Flight {
    const name = "Flight"
    const description = "Allows you to fly"

    meth onUpdate() {
        std::println("yo")
    }
}

meth test_method(target) {
    target::output()
    std::println(target::a)
}

meth main(args) {
    const flight = Flight()

    std::println(flight::name)
    std::println(flight::description)

    for (number = 0 to 6, step = 2) {
        std::println(number)
    }

    //std::println(test()[2])
}