$ "maths" -> m

container TestContainer(mut a) {
    meth test() {
        std::println(a)
    }
}

meth test() {
    std::println(":(")
}

meth main(args) {
    const cont = TestContainer(5)
    cont::test()

    cont::a = 4
    cont::test()

    std::println(std::checkType(cont, TestContainer))

    const t = Multi(WowzaClass(543))
    t::yes()

    std::println(t::wowza::a)

    const w = WowzaClass(54386545)
    std::println(w::a)
}