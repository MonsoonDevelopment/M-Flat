container Flight() {
    const name = "Flight"
    const description = "Allows you to fly"

    meth onUpdate() {
        std::println("yo")
    }
}

meth main(args) {
    const flight = Flight()

    std::println(flight::name)
    std::println(flight::description)
}