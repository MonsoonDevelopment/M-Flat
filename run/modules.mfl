container BunnyHop (name = "BunnyHop", description = "hops like a rabit") {
    meth onUpdate() {
        if (player::isOnGround()) {
            player::jump()
        }
    }
}

meth main(args) {
    const bh = BunnyHop()
    std::println(bh::name)
}

//manager::registerModule(BunnyHop())