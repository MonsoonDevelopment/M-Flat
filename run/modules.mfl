$ "maths" -> m

container TestContainer(a) {
    meth test() {
        std::println(a)
    }
}

meth test() {
    std::println(":(")
}

meth main(args) {
    const cont = TestContainer(5)
    cont::test()
}